//given an array of integers and a target sum, we've
//to generate all subsequenses that sum up to the target

#include<iostream>
using namepsace std;

void update(vector<int> &arr,int target,vector<vector<int>> &res,int sum)
{

typedef stuct t
{
	int data;
	t*left,*right;
}node,*nodep;

vector<vector<int> > Solution::combinationSum(vector<int> &A, int B) {
	vector<vector<int>> res;
	if(! A.size()) return res;
	bool x=0;
	for(int i=0;i<A.size();i++) {
		if(A[i]<=B) x=1;
	}
	if(!x) return res;
	sort(A.begin(),A.end());

}

